@model IEnumerable<ExpedientesAcademicos.Models.Materia>

@{
    ViewData["Title"] = "Index";
}

<h1>Lista Materias</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreMateria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Docente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreMateria)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Docente)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MateriaId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.MateriaId">Ver</a> |
                <a asp-action="Delete" asp-route-id="@item.MateriaId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
